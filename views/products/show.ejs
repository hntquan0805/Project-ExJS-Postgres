<!-- <h1><%= product.name %></h1>
<p>Price: $<%= product.price %></p>
<p>Description: <%= product.description %></p>
<a href="/products">Back to Products</a> -->
<!-- <div class="container">
    <div class="col-lg-12">
        <div class="d-flex align-items-center">
          <a href="/products/<%= product.id %>">
            <img class="flex-shrink-0 img-fluid rounded" src="/img/menu-<%= product.id %>.jpg" alt="" style="width: 200px;">
          </a>
          <div class="w-100 d-flex flex-column text-start ps-4">
            <h5 class="d-flex justify-content-between border-bottom pb-2 nunito-custom fw-bold text-">
                <span><a href="/products/<%= product.id %>"><%= product.name %></a></span>
                <span class="text-primary">$<%= product.price %></span>
            </h5>
            <div class="row g-3">
              <small class="col-10 playwrite-gb-s-custom"><%= product.description %></small>
            </div>
          </div>
          <button class="btn btn-primary m-2" onclick="addToCart()">
            <img src="https://www.svgrepo.com/show/488039/add-to-cart.svg" width="32" height="32" />
          </button>
        </div>
      </div>
</div> -->
<div class="container my-4">
  <div class="row justify-content-center">
    <div class="col-lg-10">
      <div class="card border-0 shadow-sm p-3">
        <div class="d-flex align-items-center">
          <!-- Image Section -->
          <a href="/products/<%= product.id %>">
            <img
              class="img-fluid rounded"
              src="/img/menu-<%= product.id %>.jpg"
              alt="<%= product.name %>"
              style="width: 150px; height: 150px; object-fit: cover;"
            />
          </a>
          <!-- Product Details Section -->
          <div class="ms-4 w-100">
            <h5 class="d-flex justify-content-between align-items-center border-bottom pb-2 mb-2 fw-bold">
              <a class="text-dark text-decoration-none" href="/products/<%= product.id %>"><%= product.name %></a>
              <span class="text-success">$<%= product.price %></span>
            </h5>
            <p class="text-muted mb-2" style="font-size: 0.9rem;">Description: <%= product.description %></p>
            <button class="btn btn-outline-primary btn-sm mt-2" onclick="addToCart()">
              <img src="https://www.svgrepo.com/show/488039/add-to-cart.svg" width="20" height="20" class="me-2" />
              Add to Cart
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
